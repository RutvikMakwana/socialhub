service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if request.auth.uid == userId;
    }
    match /tweets/{tweetId} {
      allow read: if true; // Allow anyone to read tweets
      allow write: if request.auth != null; // Allow authenticated users to write tweets
    }
  }
}
